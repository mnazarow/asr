"""Телефония: записи разговоров приезжают с АТС сами.

Речевая аналитика начинается не с распознавания, а с вопроса «где взять
записи». Пока их носят руками, разбор охватывает те разговоры, о которых
кто-то вспомнил, — то есть в основном скандальные. Здесь сервер забирает
записи с Asterisk сам, каждую, со всеми полями звонка: кто звонил, кому,
куда попал, сколько ждал, чем кончилось.

Внутри две части: `asterisk` — разговор с самой АТС (интерфейс управления
AMI, журнал звонков, поиск файла записи) и `importer` — фоновый поток,
который превращает звонок в задание распознавания и не даёт завести одно
и то же задание дважды.
"""
from __future__ import annotations

from .asterisk import AMIClient, AMIError, Звонок, разобрать_cdr_строку
from .importer import Импортёр

__all__ = ["AMIClient", "AMIError", "Импортёр", "Звонок", "разобрать_cdr_строку"]
