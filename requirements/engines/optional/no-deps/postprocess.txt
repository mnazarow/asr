# Ставится с --no-deps: сам пакет требует pynini==2.1.6.post1, а эта версия
# собирается только из исходников. Нужные зависимости перечислены рядом, в
# optional/postprocess.txt, вместе с pynini, у которой колесо есть.
nemo-text-processing>=1.0
