# Mistral Voxtral — аудио-LLM с потоковым режимом.
# Для продуктивной нагрузки рекомендуется vLLM.
#
# Первой строкой — собственный пакет движка: по ней обновление решает,
# установлен ли движок. Раньше здесь первым стоял transformers, общий с
# половиной движков, и обновление считало Voxtral установленным у всех, у
# кого есть transformers, — и каждый раз доставляло mistral-common тем, кто
# Voxtral не выбирал.
mistral-common[audio]>=1.5
transformers>=4.54
