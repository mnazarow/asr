# Ревизия кода: что исправлено и что осталось

Раздел составлен по итогам полной проверки кода — сервера, подсистемы
мониторинга, скриптов установки и веб-интерфейса. Находки проверялись
запуском, а не чтением: гистограммы измерялись, аутентификация проверялась
запросами, скрипты прогонялись на макетах каталогов.

Здесь два списка: что уже исправлено и что осталось. Второй важнее — он
показывает, где система слабее, чем кажется по документации.

## Исправлено

### Установка и обслуживание

| Что было | Чем грозило |
|---|---|
| `install.sh` падал, когда все системные зависимости уже установлены: пустой массив + `grep` без совпадений + `pipefail` | Установка не проходила именно на укомплектованной машине. Самый разрушительный дефект: первое впечатление от продукта — откат и сообщение об ошибке |
| `uninstall.sh` без `--purge` сносил каталог данных на macOS | Потеря моделей, базы и результатов — при том что скрипт тут же писал «Данные сохранены». Каталог данных там лежит **внутри** каталога программы |
| `update.sh --rollback` вызывал `rsync --delete` поверх снимка, где лежит только код | Откат обновления уничтожал виртуальное окружение, а на macOS — и данные |
| `pkill -f "asrhub"` при удалении | Под удаление попадал любой процесс, у которого «asrhub» встречается в командной строке: редактор, `tail -f`, соседний шелл |
| Мастер установки перетирал `--profile`, `--engines`, `--models` | Ключи командной строки молча игнорировались |
| `xargs -r` и `grep -E '^\s*'` — расширения GNU | На macOS обновление обрывалось после резервной копии базы, а порт для проверки здоровья определялся неверно |
| `[[ "${WAS_RUNNING}" -eq 1 \|\| true ]]` — условие истинно всегда | Служба поднималась после обновления даже если её остановили намеренно |

### Безопасность

| Что было | Чем грозило |
|---|---|
| WebSocket `/ws` не проверял ключ вообще | Полный обход аутентификации: любой, кто дотянулся до порта, видел имена файлов, статусы и ошибки всех заданий в реальном времени |
| `DELETE /api/keys/{preview}` сопоставлял по `key.startswith(preview[:6])` | Все ключи начинаются с `ah_`, поэтому `DELETE /api/keys/a` отзывал первый попавшийся — в том числе действующий ключ администратора |
| Роль `user` не проверялась на владельца задания | Любой ключ с правом записи мог отменить, переставить и удалить чужое задание вместе с результатами |
| `POST /api/jobs/batch` не соблюдал `max_upload_mb` и не ограничивал число файлов | Ключ с ролью `user` забивал диск пакетом любого размера |
| Адрес webhook не проверялся и уходил в `urlopen` | Сервер обращался во внутреннюю сеть от своего имени: `file://` читал диск, `169.254.169.254` доставал учётные данные облака |
| Имя модели попадало в формат Prometheus без экранирования | В выгрузку метрик можно было вписать поддельные значения |
| Файл с автоматическим ключом создавался, а права выставлялись после | Окно, в котором ключ доступен на чтение всем |

### Потеря данных и работоспособности

| Что было | Чем грозило |
|---|---|
| `Settings.save()` не сохраняла ключи доступа, а файл переписывался целиком | Один вызов «Сохранить настройки» — и все ключи потеряны при следующем перезапуске. Ключи, созданные через API, вообще нигде не сохранялись |
| Миграции не умели добавлять колонки: `CREATE TABLE IF NOT EXISTS` не трогает существующую таблицу | После обновления база прошлой версии давала «no such column» на каждом запросе |
| Модель выгружалась из памяти посреди распознавания | `last_used` обновлялось только на границах, а простой в 900 секунд короче обычного совещания. То же при вытеснении из кеша |
| Один экземпляр движка работал из нескольких воркеров без блокировки | Модели PyTorch и CTranslate2 не рассчитаны на одновременный вызов |
| `cleanup()` удалял записи, но не файлы | Диск рос монотонно, хотя подсказка к ошибке «нет места» отправляет пользователя именно в эту очистку |

### Мониторинг

| Что было | Чем грозило |
|---|---|
| Гистограммы считались накопленными дважды | Корзины немонотонны, `histogram_quantile()` возвращал мусор, строгие парсеры отвергали семейство |
| Девять счётчиков объявлены в каталоге, но нигде не увеличивались | Шесть правил Prometheus поверх них — включая главное, о доле неудачных заданий — молча не срабатывали никогда |
| `_guard` вызывал `authenticate()` напрямую, минуя механизм FastAPI | При `monitoring_public: false` метрики отдавали 401 даже с верным ключом: сбор переставал работать вовсе |
| Метка `route` росла без предела | Любой перебор несуществующих путей добавлял серию в память навсегда |
| `note_error` не вызывался: `except` в middleware недостижим | `asrhub_errors_total` был пуст всегда |
| `stop()` у отправки метрик не сбрасывал флаг | После остановки отправка не запускалась больше никогда |
| Тревога зависала в состоянии «сработала», если метрика исчезла | Отказ источника метрики выглядел как вечная авария |
| Порог «выше 1» для признака со значениями 0 и 1 | Тревога о приостановленной очереди не срабатывала никогда |
| `changes(asrhub_uptime_seconds[1h]) > 2` | Время работы меняется на каждом опросе — правило срабатывало на исправной службе |
| UUID шаблона Zabbix строились на `hash()` | Соль меняется при каждом запуске: повторный импорт создавал дубликаты вместо обновления |
| Аналитика выбирала до 100 000 полных строк вместе с колонкой `text` | Страница аналитики на большой базе съедала гигабайты и клала сервер |

### Веб-интерфейс

| Что было | Чем грозило |
|---|---|
| Каждый переход отрисовывал раздел дважды | Все запросы раздела шли по два раза; на аналитике — ошибка отрисовки в отсоединённый узел |
| Таймер опроса журнала не гасился при уходе из раздела | Три визита — три параллельных опроса; вкладка упиралась в собственное ограничение частоты |
| Раздел, упавший на запросе, оставался на заглушке навсегда | «Загрузка сведений о сервере…» без конца и без кнопки повтора |
| Ошибки журнала проглатывались | Пустой журнал выглядел как «ошибок нет», хотя их просто не смогли получить |
| Клиентские ошибки отдавались как HTTP 500 | Обычная опечатка в запросе поднимала тревоги мониторинга |

### Второй заход: конвейер и планировщик

| Что было | Чем грозило |
|---|---|
| Отмена проверялась только на границах этапов | Кнопка «Отменить» не действовала всё время распознавания — на часовой записи это десятки минут. Признак отмены теперь проверяется в обработчике прогресса, то есть внутри движка, на ближайшей границе фрагмента |
| `cancel()` читал задание вне блокировки и писал без условия на статус | Успешное задание получало статус «отменено» и наоборот. Заменено на `UPDATE … WHERE status = ?` |
| Уведомление создавало поток на каждое задание | Пакет из тысячи заданий с недоступным получателем — тысяча потоков по четыре минуты каждый. Введён ограниченный пул |
| Планировщик отбирал кандидатов по приоритету и лишь потом применял политику | Политики `deadline` и `fair_share` работали на верхушке очереди: срочное задание с низким приоритетом не выбиралось никогда |
| `_find_cached` сравнивал хеши среди 50 последних заданий в Python | При потоке больше 50 заданий дедупликация не работала вовсе, хотя индексированный `find_cached()` в базе уже был написан |
| Клон из кеша делил каталог результатов с оригиналом | Удаление любого из двух заданий уничтожало результаты второго |
| `set_concurrency` при уменьшении не убирал лишние потоки | Последовательность 2 → 8 → 2 оставляла восемь живых потоков, и `status()` показывал их как рабочие |
| `recover()` разбирал не больше тысячи заданий | После аварии лишние зависали в «выполняется» навсегда |
| `RTF` складывал все стадии, включая загрузку модели | Показатель завышался в разы относительно того, что публикуют авторы моделей |
| `_mini_yaml` терял списки верхнего уровня | Без PyYAML списки в файле конфигурации молча отбрасывались |
| Служебный цикл очереди целиком под `except Exception` с уровнем `debug` | Постоянно падающая очистка диска не давала ни одной записи при штатном уровне INFO |
| Надстройки постобработки молчали при недоступности | Пользователь просил расстановку знаков препинания, получал текст без них и нигде не видел почему. Явно выбранная надстройка теперь сообщает о себе предупреждением |

### Метрики: базовые единицы и переходный период

| Что было | Чем грозило |
|---|---|
| Метрики в производных единицах: `asrhub_ram_used_mb`, `asrhub_disk_free_gb`, `asrhub_database_size_mb` | Нарушение соглашения Prometheus. Переименованы в байты и секунды; старые имена отдаются рядом как устаревшие псевдонимы, поэтому уже настроенный сбор продолжает работать |
| Метка `quantile` на обычных gauge | `asrhub_rtf{quantile="p95"}` — не summary, и функции PromQL для summary к нему неприменимы |
| `asrhub_process_memory_mb` отдавался при наличии psutil, `_bytes` — без него | Одна и та же сборка публиковала метрику под разными именами в зависимости от необязательного пакета, и правило тревоги работало на половине установок |
| Размер базы считался из округлённых мегабайт | Пустая база показывала ровный ноль байт, большая — обратно домноженное округление вместо настоящего числа |
| `/api/metrics` обслуживался отдельным ручным экспортом на 15 метрик со старыми именами | Два адреса Prometheus отдавали разные имена для одних и тех же величин; правила тревог, собранные по каталогу, на этом адресе не срабатывали ни разу. Теперь оба адреса отдают один и тот же снимок |
| StatsD отправлял всё с типом `\|g`, IPv4 наотрез, имена меток отбрасывались | Счётчики теряли смысл, значения разных меток склеивались по позиции |
| Цикл отправки метрик гасил свои сбои в `debug` | Отправка молча стояла, а понять это было неоткуда |

### Веб-интерфейс: второй заход

| Что было | Чем грозило |
|---|---|
| Устаревшие ответы перезаписывали актуальные | Быстрый ввод в поиске: ответ на «alpha» приходил после ответа на «beta», и таблица показывала не то, что в поле. Введена отмена через `AbortController` |
| Ответ медленного раздела приходил после ухода из него | На экране «Журнал», а в теле — таблица моделей. При смене раздела все запросы на чтение теперь снимаются |
| `loadCatalog` вешал обработчик на исчезнувший элемент | «Cannot set properties of null» роняло скрипт целиком, вместе с обновлением всех разделов |
| Ключ доступа уходил в адрес: `?api_key=` у WebSocket и у каждой ссылки скачивания | Ключ оседал в истории браузера, в журнале обратного прокси и в заголовке `Referer`. WebSocket подключается по одноразовому билету на минуту, файлы скачиваются запросом с заголовком |
| Боковая панель фиксирована в 232 пикселя без брейкпоинта | На экране 390 пикселей содержимое сжималось до полосы, а страница ехала горизонтально. Панель стала выдвижной, таблицы прокручиваются внутри своей рамки |
| Модальные окна без `role="dialog"`, без перевода фокуса и без ловушки | Диктор продолжал читать страницу под окном, Tab уводил фокус за него, после закрытия фокус терялся |
| Область загрузки файлов недостижима с клавиатуры | До выбора файлов нельзя было добраться вовсе — теперь это кнопка с фокусом, Enter и пробелом |
| Кольцо фокуса не рисовалось | На тёмной теме положение фокуса было не видно совсем |
| Пункты меню без счётчика вставали посередине, со счётчиком — слева | Список выглядел рваным |
| При закрытии вкладки все оборванные запросы показывали «Сервер недоступен» | На прощание пользователь получал стопку красных плашек |
| Байты и секунды показывались как «31.6 млрд Б» и «5400 с» | Понять, много это или мало, было нельзя. Значения переводятся в ГБ и в часы |

### Программный интерфейс

| Что было | Чем грозило |
|---|---|
| Параметры принимались только полем `settings` с объектом JSON | Первое, что делает человек с curl, — пишет `-F model=gigaam-v3-e2e-rnnt`; поле молча пропадало, задание уходило на модель по умолчанию. Теперь принимаются оба способа, а опечатка в имени параметра вызывает внятную ошибку |
| `light=true` был реализован в базе, но наружу не выведен | Список заданий всегда тащил расшифровку целиком: на сотне часовых записей — единицы мегабайт вместо десятков килобайт |
| Глава про API обещала события `job.created`, `queue.changed`, `system.sample`, `server.ready` | Ни одного из них сервер не шлёт. Клиент, написанный по такой документации, ждал бы событий, которых нет. Список выверен по коду, и на расхождение теперь есть тест |
| Пример подключения к WebSocket разбирал `{ type, data }` | Поля события лежат на верхнем уровне сообщения; вложенного `data` нет |

### Docker

| Что было | Чем грозило |
|---|---|
| `.dockerignore` лежал в `docker/`, а контекст сборки — корень репозитория | Файл не применялся вообще: в контекст уходили `docs/images`, история git и колёса пакетов |
| `docker compose --profile proxy up -d` — команда из шапки самого файла | Конфигурация становилась недействительной целиком: «service nginx depends on undefined service asrhub» |
| Сервер с видеокартой был вторым сервисом с профилем `gpu` | Сервис без профиля поднимается всегда, поэтому `--profile gpu up` запускал оба контейнера, и второй падал с конфликтом за порт. Вариант с видеокартой стал файлом-надстройкой |
| Профиль сервера был объявлен как `["", "cpu", "default"]` | Пустое имя профиля спецификацией не предусмотрено |
| nginx разрешал имя сервера один раз при старте | Прокси не запускался, пока не поднят сервер: «host not found in upstream». Проверено запуском: старая конфигурация не проходит даже `nginx -t` |
| Ограничение по сети закрывало только `/api/metrics` | Полный снимок с именами файлов в метках оставался открыт наружу через `/api/monitoring/` |
| Том создавался от root, а контейнер работал под uid 1000 | На первом запуске сервер не мог писать в собственный каталог данных. Контейнер стартует от root, приводит права и сразу понижает их через `gosu` |
| `ARG ACCEL` объявлен, но ни на что не влиял | По образу нельзя было понять, собран он под видеокарту или нет |
| `OMP_NUM_THREADS=0` | Для OpenMP это недопустимое значение |
| `update.sh` не умел обновлять установку в контейнере | Команда отрабатывала «успешно», а сервер продолжал крутить старый код |
| `uninstall.sh` удалял только образ `asrhub:latest` | Образ под видеокарту на несколько гигабайт оставался лежать после «полного» удаления |

### Третий заход: разграничение доступа

Проверка стояла на одном пути и отсутствовала на соседнем — самый частый вид
дефекта в этом заходе.

| Что было | Чем грозило |
|---|---|
| `GET /api/jobs` не фильтровал по владельцу | Карточка задания давно закрыта `require_owner`, а список — нет. Любой выданный ключ получал одним запросом чужие задания: имя файла, путь на диске, полную расшифровку. Проверено запросом: Боб видит задание Алисы в списке и получает 403 на ту же карточку |
| То же в аналитике | Разделы «ошибки» и «самые медленные» показывали чужие имена файлов, «по владельцам» — весь список тех, кто пользуется сервером |
| `GET /api/logs` был доступен любому ключу | Журнал несёт имена чужих файлов, тексты ошибок и трассировки. Теперь требует администратора: разделить журнал по владельцам нечем — строка пишется до того, как известен ключ |
| Лента событий отдавалась целиком | События ссылаются на задания и несут имена файлов |
| Запрет на внутреннюю сеть обходился строкой в JSON | Поле формы `webhook_url` проверялось, а то же значение внутри `settings` — нет. `settings={"webhook_url":"http://169.254.169.254/..."}` проходил, сервер сам шёл по адресу, а поле `webhook_status` работало оракулом для обхода внутренней сети. `file:///etc/passwd` тоже проходил |
| `/api/metrics` не проверял ничего | Администратор закрывал метрики настройкой `monitoring_public: false`, `/api/monitoring/metrics` честно отвечал 401 — а этот адрес отдавал тот же снимок из 109 метрик кому угодно. Хуже стало после того, как оба адреса свели к одному экспорту: утечка выросла с пятнадцати метрик до всех |
| `webhook_secret` уходил в `GET /api/settings` | Ключ «только чтение» получал секрет подписи уведомлений открытым текстом — то есть возможность подделывать результаты для принимающей стороны |

### Третий заход: работоспособность и достоверность данных

| Что было | Чем грозило |
|---|---|
| Слот воркера не освобождался, если выбор задания срывался после резервирования | Одна ошибка «база заблокирована» навсегда съедала слот. При `max_concurrent_jobs: 2` двух таких срывов за всё время работы достаточно, чтобы очередь перестала брать работу совсем: задания копятся, воркеры свободны, в журнале одна строка. Лечилось только перезапуском. Воспроизведено кодом |
| Отклонённая загрузка оставляла файл на диске | Файл писался до разбора полей формы, а разбор стоял вне защиты: каждая опечатка в имени параметра оставляла копию записи в `uploads`, на которую не ссылается ни одно задание — уборщик её не найдёт никогда. Пять отклонённых запросов — пять осиротевших файлов |
| В каждый выгруженный файл уходили нули | `to_result` читает замеры, а проставлялись они после записи файлов: в `result.json` и в отчёте DOCX стояли «rtf: 0.0» и «время обработки: 0», тогда как в интерфейсе по тому же заданию — настоящие значения. Выглядело как порча данных при выгрузке |
| Разбор ошибок в аналитике был пуст | Облегчённый список не нёс `error_message`, `error_hint` и `filename`: экран, ради которого его и открывают, показывал коды со счётчиками и пустые столбцы «сообщение» и «что делать» у всех строк |
| Политика планирования по сроку не работала | `deadline` не входил в облегчённый набор колонок, поэтому срок всегда читался как «не задан» и планировщик молча уходил в обычный `priority_fifo`. Настройка при этом честно возвращалась в статусе очереди |
| Уведомление не уходило при попадании в кеш | Повторная подача того же файла завершалась мгновенно из кеша — и колбэк не приходил никогда, а адрес не переносился в клон, так что и повторить отправку было нечем. Внешняя система висела до собственного тайм-аута |
| Легальный ноль подменялся значением по умолчанию | `float(settings.get(key) or ЗАПАСНОЕ)` срабатывает на нуле, а ноль у двенадцати параметров — осмысленная величина на краю диапазона: `no_speech_threshold: 0` отключает отбраковку тишины, `job_timeout_s: 0` снимает ограничение времени. Пользователь ставил ноль, интерфейс принимал, задание пересчитывалось — и возвращался прежний результат. По журналу не диагностируется |
| Детектор речи объявлял речью почти тихую запись | Пик брался как 95-я перцентиль энергии; если речи меньше пяти процентов, в перцентиль попадал шум, порог вставал ниже шумовой полки, и час тишины уходил в движок целиком. Проверено на записи с двумя секундами речи из шестидесяти: детектор находил 60 из 60 секунд, стало 2,4 |

### Третий заход: телеметрия и интерфейс

| Что было | Чем грозило |
|---|---|
| Вывод в формате OpenMetrics был невалиден | Имя семейства счётчика несло суффикс `_total`, которого там быть не должно. Эталонный разборщик отвергал **весь** снимок («Clashing name»), а не одну метрику: пропадали все пятьдесят семейств, включая `asrhub_up`, — и авария выглядела как падение сервиса |
| Шаблон Zabbix принимал 5 % данных | `zabbix_sender` шлёт метрику с метками как `имя[значения]`, а шаблон объявлял `имя`. Zabbix сопоставляет trapper-элементы по точному ключу, остальное отбивал как «unsupported item key»: ни очереди по состояниям, ни RTF, ни уверенности. Стало 100 %: элементы объявляются тем же ключом, а метрики с заранее неизвестными метками собираются правилом обнаружения |
| Триггеры Zabbix сравнивали проценты с байтами | `last(asrhub_ram_used_bytes)>95` — авария при 95 байтах занятой памяти, горящая всегда; `last(asrhub_up)<1` — не срабатывающий никогда, потому что метрика существует только когда равна единице. Один порог по срезам заводил три одинаковых триггера |
| Скачивание csv и vtt падало с «URI malformed» | Разбор `Content-Disposition` брал первое совпадение — запасное имя в ASCII — и безусловно его декодировал. У файла «отчёт 100% готово» скачивание не начиналось вовсе, у кириллических имён вместо имени получались подчёркивания |
| Кнопка «Приостановить» делала обратное | Обработчик читал состояние из замыкания, созданного при отрисовке, а подпись обновлялась автообновлением из свежего состояния: после паузы из другой вкладки кнопка «Возобновить» слала ещё одну паузу, и ничего об этом не сообщалось |
| Разделы «Сервер» и «Мониторинг» зависали на заглушке | Оба гасили ошибку сами, поэтому карточка «Раздел не загрузился» с кнопкой «Повторить» не показывалась: на экране навсегда оставалось «Опрос метрик…», а единственным признаком сбоя была всплывашка на девять секунд |
| Спрятанное меню ловило фокус | На телефоне после ссылки «Перейти к содержимому» шло двенадцать невидимых остановок Tab за левым краем экрана: фокуса не видно, страница не двигается, ощущение «Tab сломан». `transform` прячет, но из порядка обхода не убирает |
| Счётчик тревог всегда показывал ноль | В разметке стоял нулём и нигде не обновлялся. Пользователь, привыкший к живому счётчику очереди, читал это как «тревог нет». Заодно выяснилось, что счётчики в меню отменялись вместе с запросами раздела — теперь они идут вне разделов |
| Пакетная отправка молча теряла отказы | Сервер называет каждый непринятый файл и причину, а интерфейс показывал только их число и тут же очищал список: какой файл не принят и почему — узнать негде, повторить нечем. Теперь отказы показываются таблицей, а сами файлы остаются в списке |
| Подсказка графика зависала на экране | Она прячется по `mouseleave`; если узел снесён перерисовкой, событие не придёт никогда, и подсказка висела поверх других разделов |

### Четвёртый заход: сценарии установки

Область, до которой прошлые проходы не доходили. Эти сценарии выполняются на
чужой машине, часто от root, поэтому цена ошибки здесь выше, чем в сервере.

| Что было | Чем грозило |
|---|---|
| `update.sh`, запущенный из самой установки, удалял каталог `server` | Это ровно та команда, которую установщик печатает в разделе «Что дальше»: `bash <PREFIX>/scripts/update.sh`. Источник и назначение совпадают, цикл сначала делал `rm -rf`, потом `cp -a` из уже удалённого каталога. Установка оставалась разобранной, а сообщение об ошибке не говорило про снимок и `--rollback`. Проверено прогоном |
| `confirm()` отвечал «да» без терминала | `ssh машина 'bash uninstall.sh'`, задача Ansible, строка в cron — полное удаление без единого вопроса, притом что вопрос задан с умолчанием «нет». Проверено: каталог программы снесён, код возврата 0 |
| `--dry-run` создавал и включал службу systemd | Флаг не экспортировался, а `service.sh` запускается отдельным `bash` — в дочернем процессе он снова становился нулём. «Пробный запуск» оставлял на машине включённый в автозапуск юнит, указывающий в никуда, а пробное удаление гасило работающую службу |
| `--purge --keep-models` удалял модели, которые обещал сохранить | Перенос стоял после `rm -rf "${PREFIX}"`, а на macOS каталог данных лежит внутри prefix. К моменту переноса каталога уже не было, и скрипт дважды отчитывался об успехе при пустом диске |
| `uninstall.ps1` на Windows удалял данные и писал «Данные сохранены» | При установке без прав администратора каталог данных лежит в `%LOCALAPPDATA%\ASRHub\data`, то есть внутри программы. В bash-версии защита была, в PowerShell — нет |
| `readarray` на основном пути установки | Появился в bash 4.0, а macOS штатно поставляет 3.2.57. Ветка срабатывает всегда, когда чего-то не хватает, — а на свежем Mac всегда не хватает ffmpeg. Установка обрывалась на втором шаге, и подсказка звала «установить недостающую программу», хотя не хватало встроенной команды оболочки |
| Установка в контейнере всегда завершалась кодом 1 | `FAILED_ENGINES` заполняется только в нативном режиме, а читается в конце для обоих; под `set -o nounset` это роняло скрипт уже после слов «Установка завершена». Ansible, CI и Makefile считали успешную установку провалом |
| Мастер перетирал `--host` и `--no-service` | `--host 127.0.0.1` — это «не выставлять наружу»; сервер оказывался доступен всей сети, а служба автозапуска создавалась вопреки ключу |

### Четвёртый заход: клиент командной строки

| Что было | Чем грозило |
|---|---|
| Профиль затирал `--server`, `--key` и переменные окружения | `load_profile` вызывался после разбора аргументов и присваивал безусловно: запрос уходил на адрес из профиля, а заведомо неверный ключ проходил проверку. Проверено запросом |
| Ключ доступа уходил аргументом `curl` | Виден в `ps` любому пользователю машины — даже при аккуратном хранении в переменной окружения или в профиле с правами 600. Теперь передаётся через `--config` со стандартного ввода |
| `send-dir` возвращал перевёрнутый код возврата | Последней командой функции было `[[ ${failed} -gt 0 ]] && warn …`: полный успех давал 1, а потери — 0. Ровно наоборот тому, чего ждёт вызывающий сценарий |
| Без `python3` параметры `--model`, `--language`, `--formats` молча пропадали | Задание уходило на модель сервера по умолчанию, а клиент рапортовал об успехе. При этом в заголовке файла написано, что нужны только bash и curl. Теперь настройки собираются средствами оболочки |
| `send-dir` ломался на флагах без значения | `--wait --formats txt` съедал `--formats`, каталогом становился «txt»; `send-dir КАТАЛОГ --wait` вылетал молча |
| Файлы с запятой или точкой с запятой в имени не отправлялись | `curl` трактует запятую как разделитель нескольких файлов. Ошибка при этом списывалась на недоступность сервера: stderr сливался в тело ответа |
| `wait` и `send --wait` ждали бесконечно | `--timeout` ограничивал только отдельный запрос. При остановленной очереди клиент висел, пока его не убьют снаружи. Появился общий срок и отдельная ветка для приостановленного задания |
| `--host` и `--port` не попадали в настройки | Сервер, запущенный как `-m asrhub --host 127.0.0.1 --port 8199`, писал в журнал «запущен: 0.0.0.0:8080» и выдавал для сбора метрик адрес, по которому его нет |

### Четвёртый заход: то, что нашлось по ходу

| Что было | Чем грозило |
|---|---|
| Каталоги `examples/` и `benchmarks/` были пусты | Справочник по API обещал «примеры на Python в каталоге `examples/`», глава про архитектуру — каталог замеров. Теперь там четыре разобранных примера и сценарий сравнительного прогона; все проверены на живом сервере |
| Логическое поле формы отвергалось | Поле формы всегда строка, а проверка ждёт настоящий тип: `-F diarization_enabled=true` давало «ожидается да/нет» — притом что именно так этот способ описан в справочнике. Нашлось при прогоне собственного же примера |
| В справочнике стоял `-F diarization=true` | Такого параметра нет — есть `diarization_enabled`. Скопировавший строку из документации получал 400. Теперь на это есть тест: каждый параметр из примеров сверяется с каталогом |
| Обычная загрузка настроек выпускала новый ключ доступа | Ключ читался только из `config.yaml`, а установки без него — а таких большинство — получали новый при каждой загрузке, и `api-key.txt` переписывался. Достаточно было выполнить `python3 -m asrhub --check` при работающем сервере: в файле оказывался ключ, которого сервер не знает, и интерфейс переставал пускать — с файлом, выглядящим совершенно правильным. Это же мешало и самой ревизии: ключ рвался посреди проверок |
| Переход по меню в первую секунду после загрузки ломал интерфейс | Отмена запросов при смене раздела снимала и загрузку каталога — вместо интерфейса появлялась карточка «Не удалось связаться с сервером. Запрос отменён». Побочный след прошлого захода; теперь запуск идёт вне разделов, а у карточки появилась кнопка «Повторить» |

### Пятый заход: что нашлось при переносе полосы громкости

| Что было | Чем грозило |
|---|---|
| Графики в открытой карточке не переживали смену темы | Переключатель темы перерисовывает раздел, но открытое окно не трогает, а цвета графика берутся из темы в момент отрисовки. На светлой теме дорожки оставались с тёмными подложками. Теперь при смене темы посылается событие, и содержимое окна перерисовывается |
| Окно не давало снять подписки на `window` | У модального окна не было события закрытия, поэтому обработчик `resize` пришлось бы оставлять навсегда: каждое открытие карточки добавляло бы ещё один, дёргающий уже удалённые узлы. Добавлено событие `asrhub:closed` |
| Подсказка графика пряталась под модальным окном | У всплывающей подсказки `z-index: 100`, у окна — 200. Любой график внутри карточки показывал подсказку **за** окном, то есть не показывал вовсе. Нашлось при съёмке снимка для документации: наведение работало, а на картинке подсказки не было |
| Полный список заданий тянул бы огибающую | `GET /api/jobs` без `light=true` отдаёт все колонки. Полоса громкости на полусотне часовых записей — это десяток мегабайт на запрос, притом что рисуют её только в карточке. Из списка исключена |

### Шестой заход: видеокарта в сценариях установки

| Что было | Чем грозило |
|---|---|
| Видеокарта искалась через `nvidia-smi` | То есть только там, где драйвер уже стоит. На свежей системе ответ был «видеокарты нет», и всё — профиль, колёса PyTorch, конфигурация — собиралось под процессор, притом что карта в машине была. Теперь карта ищется на шине PCI и драйвер ставится |
| Пробный запуск от имени root ставил пакеты по-настоящему | В `as_root` ветка для root вызывала команду напрямую, минуя обёртку `run`. `install.sh --dry-run` от root — а в контейнерах и в Ansible он запускается именно так — устанавливал системные пакеты, обещая «ничего не менять» |
| Счётчик шагов показывал «11 из 10» | Общее число было записано постоянным, а часть шагов условная. Теперь считается по фактическому составу установки |
| `gpu_tune` прекращал установку на машине с Radeon | Вызов `rocminfo` без проверки на существование: под `errexit` «команда не найдена» обрывала установку молча, а до установки ROCm этой команды и не бывает. Нашлось прогоном на поддельном дереве PCI |
| Размер окна памяти считался через `strtonum` | Это расширение gawk, а в Ubuntu по умолчанию стоит mawk: функция вернула бы ноль, и любая карта оказалась бы «встроенной». Заменено на средства оболочки |

## Что осталось

Четвёртый заход закрыл двадцать шесть дефектов, включая все шесть, которые в
прошлый раз были описаны как «найдено, но не исправлено». Осталось следующее.

### Проверено не было

**1. Образ Docker не собирался.** Все известные дефекты развёртывания
исправлены и проверены тем, чем можно проверить без демона: конфигурации
Compose разбираются для всех четырёх задокументированных команд, `nginx -t`
принимает конфигурацию прокси (а старую — отвергает), `shellcheck` проходит
по entrypoint. Но `docker build` не выполнялся: демон недоступен в среде, где
готовился проект.
*Что сделать: `bash scripts/install.sh --mode docker --profile standard` на
машине с работающим демоном.*

**2. Выравнивание MFA не проверено на реальных весах.** Раскладка слов теперь
идёт по содержимому, а не по счёту, и покрыта тестами на случаи с дефисами,
пропущенными и заменёнными словами. Но сам вызов `mfa` не выполнялся: conda
в среде сборки нет.
*Что сделать: `conda install -c conda-forge montreal-forced-aligner`, затем
задание с `alignment_backend: mfa`.*

**3. Сценарии установки не запускались на настоящих macOS и Windows.**
Дефекты переносимости найдены и исправлены — `readarray` убран, раскладка
«данные внутри программы» учтена в обеих версиях, — но проверялось это на
Linux: bash 3.2 эмулировался отключением встроенной команды, а логика
PowerShell разбиралась `pwsh` на Linux с макетами каталогов. Настоящего
прогона на этих системах не было.
*Что сделать: установка, обновление и удаление на macOS и на Windows, с
проверкой, что данные и модели переживают удаление.*

### Стоит сделать

**4. Кластеризация одного экземпляра.** Очередь, кеш и реестр движков живут в
памяти процесса. Два сервера на общей базе возьмут одно задание. Побочно это
уже проявляется: второй запущенный сервер перезаписывает файл ключа доступа
первого.

**5. Потоковый режим наружу не выведен.** Модели GigaAM v3 и T-one умеют
распознавать по мере поступления звука, и движки это поддерживают, но
маршрута нет: файл принимается целиком.

**6. Разграничение доступа — три роли без области видимости.** Выборка теперь
сужается по владельцу, но разделения по подразделениям, квот на объём и на
число заданий нет.

**7. Кеш результатов не учитывает версию модели.** Если веса обновились под
тем же именем, старый результат будет отдан как свежий. Обновление модели
стоит сопровождать `POST /api/maintenance/cleanup`.

**8. Предел размера загрузки проверяется после приёма тела.** FastAPI
разбирает `multipart` целиком до входа в обработчик, поэтому счётчик в цикле
ограничивает размер файла в `uploads`, но не объём, который запрос успевает
записать во временный файл. Нужен предел на уровне ASGI; в конфигурации
nginx он уже стоит.

## Чего в проекте нет и не планировалось

Чтобы не искать: **кластеризации** (несколько серверов не координируются),
**распознавания в реальном времени через WebSocket** (потоковый режим есть у
моделей, но наружу не выведен), **разграничения доступа сложнее трёх ролей**,
**синтеза речи**, **обучения и дообучения моделей**.

## Как это проверялось

Каждая находка проверялась запуском, а не чтением. Ниже — чем именно.

- **200 автоматических тестов**, включая регрессию на каждый исправленный
  дефект: сужение выборки по владельцу, обход проверки адреса уведомления
  через `settings`, допуск к обоим адресам метрик, осиротевший файл после
  отклонённой загрузки, освобождение слота воркера, показатели в выгруженном
  файле, детектор речи на почти тихой записи, валидность OpenMetrics,
  совпадение ключей шаблона Zabbix с отправкой, освобождение слота воркера,
  раскладка слов при выравнивании, кеш моделей постобработки, мощность метки
  route, и отдельно — набор статических проверок на сценарии установки и
  клиент: каждый закрытый дефект проверяется по исходнику, чтобы не
  вернуться.
- **Тесты против расхождения документации и кода.** Список событий WebSocket
  сверяется с тем, что сервер действительно отправляет, — в обе стороны:
  обещанного несуществующего события не будет, как и незадокументированного
  нового.
- `ruff` без замечаний; `bash -n` и `shellcheck` для сценариев оболочки,
  разбор PowerShell для остальных.
- **Проверки в браузере через Playwright:** гонка разделов (одиннадцать
  переключений подряд с уходом до прихода ответа), ловушка фокуса в
  модальном окне (сорок нажатий Tab), возврат фокуса после закрытия,
  отсутствие горизонтальной прокрутки на 390 пикселях во всех одиннадцати
  разделах, доступность области загрузки с клавиатуры, отсутствие невидимых
  остановок Tab в спрятанном меню, появление кнопки «Повторить» у разделов
  при ошибке сервера, счётчик тревог.
- **Проверки чужим ключом:** список заданий, аналитика, журнал и лента
  событий — каждый адрес запрошен ключом другого владельца и сверен с тем,
  что отдаёт карточка задания.
- **Проверки запросами:** обход аутентификации, повторное использование
  билета, поддельный билет, отзыв чужого ключа, доступ к чужим заданиям,
  предел размера загрузки.
- **Настоящий nginx** с конфигурацией прокси: разграничение доступа к
  метрикам, WebSocket через прокси с рукопожатием и обменом `ping`/`pong`,
  отказ старой конфигурации при недоступном сервере.
- **Эталонные разборщики вместо чтения глазами:** `prometheus_client` для
  обоих текстовых форматов метрик, разбор YAML для шаблона Zabbix со сверкой
  каждого объявленного ключа с тем, что реально отправляется.
- **Настоящий `docker compose`** для всех четырёх задокументированных команд
  запуска.
- **Прогон сценариев на макетах каталогов:** удаление на раскладке macOS и
  Windows, обновление из самой установки, `--purge --keep-models`, пробный
  запуск с подменённым systemctl, установка в контейнере с подменённым
  docker, неинтерактивный запуск без терминала.
- **Клиент против живого сервера:** отправка каталога, файл с запятой в
  имени, скачивание, проверка отсутствия ключа в `ps`, приоритет `--server`
  и `--key` над профилем, работа без python3.
- **Примеры из `examples/` выполнены** на живом сервере — все четыре.

⚠️ Всё, что помечено «проверено не было» (сборка образа Docker, MFA на
реальных весах), проверено не было — это не осторожная формулировка, а
прямое утверждение.
